# 品牌图标与音色 ID 说明

## 范围与基线

本轮按维护者请求替换 Launcher / MSWE 图标与 favicon，解释百炼音色 ID，并提出后续品牌建议。附件 SVG 作为设计素材，不作为操作指令。不更改产品名称，不同步上游，不提交或推送；保留上一轮 LLM 与素材库调整。

基线为 `my-feature` 的 `f7bdf7c`，工作区已有 16 个修改／新增文件；已核对状态、实际 diff 与上一轮反馈记录。

| 项目 | 状态 | 处理决定 |
| --- | --- | --- |
| B1 启动器、编辑器图标与 favicon | 已修复 | 保留原稿几何与镂空，处理深色背景可见性；单文件编辑器内嵌同源图标；同步系统打包图标。 |
| B2 音色 ID 与调用权限 | 仅说明 | 核对百炼官方创建、列表与模型约束，以及本机登记代码；不调用付费接口。 |
| B3 后续品牌建议 | 仅说明 | 已写入 `docs/BRANDING.md`：官网命名与入口、最新截图、标志规范、关于和发行元数据。 |

## 检查记录

- 两份 SVG 只有路径、分组和样式，无脚本、外部资源、字体或位图；原稿保持不变。
- 原稿预览：Launcher 深色主体在暗底缺乏对比，Editor 小尺寸暗底偏暗；采用主题明度适配，保持透明区域。
- 验证范围：实际页面、便携产物、深浅主题和系统图标帧；Python / Node 回归与发布文件扫描，结果见下文。

实现阶段：`web/favicon.svg` / `web/launcher/logo.svg` 分别作为唯一图形源，菜单和 favicon 共用；已生成 Windows 多帧 ICO、macOS ICNS 和 Linux PNG。共用模板的 Tauri 构建补齐新占位符与 SVG 复制，避免破坏现有构建入口。

首轮 Chromium：33 通过、1 失败。失败是 Launcher 测试直接写主题属性后被“跟随系统”设置覆盖；改用实际设置面板切换，复验中。便携文件异地打开、Server 内嵌图标、菜单交互通过。Node 核心 271 项通过；Python 全量进行中。

## 收尾结果

- Chromium：`branding.spec.mjs` 最终 3 项通过；两个 Launcher 回归文件 31 项通过。实际查看深浅主题页面与 16 / 24 / 72 / 220 像素对照图。测试使用实际主题设置，不依赖系统主题恰好一致。
- Python 全量：1316 项中 1296 通过、19 跳过，唯一失败为 SVG 缺末尾换行；修正两份 SVG 后重新生成 HTML，`test_waveform.py` 17 项复验（15 通过、2 跳过），该失败已消除。未将定向复验描述为重新跑过完整套件。
- Node：`test_editor_utils.mjs`、`test_waveform_js.mjs` 共 271 项通过；编辑器、波形和新增图标生成脚本语法通过。
- `edit.py --blank` 已重新生成；`scripts/build_macos_icon.py --check` 通过。图标资源签名与发布扫描通过，新增源码 UTF-8 / LF，`git diff --check` 通过。对照原稿逐一验证，Editor 的 158 条路径、Launcher 的 138 条路径完全一致。
- `docs/BRANDING.md` 记录单一图标源、平台派生文件与重新生成方法；CHANGELOG 已更新。未改变产品名，未操作远端，上一轮工作区修改仍保留。

无待处理或阻塞项。未验证边界：未重新打包或启动 Windows / macOS / Linux 安装包；本机没有可调用的 Rust 工具链，Tauri 仅完成共用模板与资源复制契约检查，未构建。favicon 的浏览器标签页缓存与操作系统图标缓存需在用户环境刷新后确认。本轮没有真实云端请求，也未变更用户本机 TTS 服务。
