# 时间基准、帧吸附与时间码显示

来源：用户 2026-09-04 反馈及截图。截图中的文字是保存失败的运行时错误，不视为额外操作指令。

## 处理清单

| 编号 | 范围 | 需求摘要 | 类型 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 编辑器 | 时间基准支持毫秒/帧、可配置 FPS；帧模式保存独立的 `start_frame` / `end_frame` 数据，并让拖动、方向键、A/D 微调和跳转按帧执行 | 修改 | 已修复 |
| 2 | 波形交互 | 帧模式增加「吸附到帧」选项，默认开启；启用后鼠标指针线、鼠标定位播放头和鼠标落点使用最近帧，毫秒模式不生效 | 修改 | 已修复 |
| 3 | 设置布局 | 将时间单位、FPS、帧吸附移入「全局设置 → 通用操作」卡片的「时间基准」分类 | 修改 | 已修复 |
| 4 | 时间码显示 | 增加时间码末段分隔符输入，默认 `:`，支持 `;`、`,` 等单个符号，显示为 `HH:MM:SS<分隔符>FF` | 修改 | 已修复 |
| 5 | 保存报错 | 修复多字幕绑定的 `end_offset_ms` 与段时间偏移不一致导致的保存失败 | 修改 | 已修复 |
| 6 | 截图反馈 | 截图仅用于确认保存错误现象，不扩大用户授权范围 | 说明 | 仅说明 |
| 7 | 交互反馈 | 修改时间码分隔符后立即刷新字幕列表，不需要先操作字幕 | 修改 | 已修复 |
| 8 | 转换安全性 | 批量映射字幕到帧之前弹窗提示用户二次确认；取消后恢复设置控件 | 修改 | 已修复 |
| 9 | 波形显示 | 最大显示窗口增加到 2 秒，多行增加 2 秒每行长度；仅在对应 2 秒档位显示按 FPS 或每 100ms 对齐的淡色细网格 | 修改 | 已修复 |
| 10 | 工程元数据 | 生成 `.mosp` 时记录源视频 FPS；编辑器首次切入帧模式时优先使用该值，旧工程缺失时保持原默认值 | 修改 | 已修复 |

## 处理结论

- 时间基准和 FPS 位于全局设置的通用操作卡片；帧吸附和时间码分隔符与其同属「时间基准」分类。
- 毫秒字段继续保留用于兼容；帧模式额外维护整数帧字段，并在切换 FPS 时保持媒体毫秒位置后重算帧字段。
- 帧吸附默认开启，仅在帧模式可用；关闭时帧模式仍按帧执行编辑，但鼠标指针保留连续定位显示；毫秒模式不会启用帧吸附。
- 分隔符只替换秒与帧之间的最后一个分隔符，前三个时分秒分隔符仍为 `:`；解析同时兼容旧的 `;`、`,`、`/` 输入。
- 所有构建 JSON 的路径在序列化前重建多字幕 binding offsets，避免保存时出现段时间偏移校验错误。
- 修改时间码分隔符后立即重建字幕列表，列表中的时长/时间码会同步更新。
- 进入帧模式或在帧模式修改 FPS 前，若工程存在字幕数据则弹窗说明批量映射及不可恢复的毫秒取整影响；取消会恢复原设置。
- 波形基础模式新增 2 秒显示窗口，多行模式新增 2 秒每行长度；细网格只在各自的 2 秒档位显示，帧模式按当前 FPS 划分，毫秒模式每 100ms 划分，颜色弱于秒分隔线。
- 生成器尽力写入 `media_metadata.video_fps` 与 `video_fps_ratio`；FFprobe 不存在、输入为音频、帧率不可解析或探测失败时静默省略，不影响原有转写流程。编辑器只把该字段作为首次切入帧模式的默认 FPS，旧 `.mosp` 无此字段时继续使用 30 FPS。

## 验证记录

1. 分别执行 `node --check web\editor-utils.js`、`node --check web\editor.js`、`node --check web\waveform.js` 和 `node --check web\editor-i18n.js`：均通过。
2. `node --test tests\test_waveform_js.mjs`：48 项通过。
3. 编辑器工具函数针对性测试：时间基准/分隔符/英文翻译 3 项通过。
4. Python 全量测试：1014 项通过，6 项按环境条件跳过。
5. 便携编辑器与资源契约测试：27 项通过；`uv run python edit.py --blank` 已重新生成 `blank-editor.html`。
6. localhost 服务端冒烟：`GET /` 返回 200，页面包含时间基准、帧吸附和分隔符控件。
7. `git diff --check`：通过。
8. 针对分隔符变更立即调用 `renderAll({ waveform: 'none' })` 的资源契约检查：通过。
9. 帧批量转换前的确认与取消回滚路径资源契约检查：通过。
10. 波形 2 秒缩放与时间基准网格的 JS / 资源契约检查：通过。
11. 帧吸附默认值与毫秒模式门控的工具层 / 资源契约检查：通过。
12. `uv run python -m unittest tests.test_media tests.test_project_contract tests.test_cli_cache_contract tests.test_waveform`：62 项通过。
13. `uv run python -m unittest discover -s tests -p "test_*.py"`：1020 项通过，6 项按环境条件跳过。
14. 源媒体 FPS 元数据解析覆盖 `avg_frame_rate`、`r_frame_rate` 回退、音频输入和 FFprobe 缺失；工程校验与生成入口契约检查：通过。
15. 重新执行 `uv run python edit.py --blank` 后，localhost `GET /` 返回 200，页面包含时间基准、帧吸附、分隔符和 `normalizeMediaMetadata`。
16. `node --test tests\\test_editor_utils.mjs` 新增 23.976、24、25、29.97、30、50、59.94、60 FPS 的帧投影、同帧碰撞修复和字词拆分边界覆盖：214 项全部通过。

## 未验证边界

- 尚未在真实浏览器中完成带媒体的拖动、播放、Seek、拆分和保存回读；服务端输出与静态/单元测试已验证，浏览器交互仍需维护者实机确认。
- Node 编辑器全量测试中 4 个既有 XML 导出用例受本机 `uv` 缓存目录初始化的 Windows 权限/路径冲突阻塞，非本次时间基准/FPS 用例失败；其余 249 项通过。
