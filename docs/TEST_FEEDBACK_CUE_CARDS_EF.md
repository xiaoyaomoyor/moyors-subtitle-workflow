# 字幕卡片与波形 E/F 阶段

2026-09-14：按用户补充调整预设配色、真实边缘手柄和共享边界；完成三层贴片限制，验证后将 A–F 及既有编辑器改动提交并推送至 origin/my-feature。

已核对 AGENTS.md、C/D 记录、工作区状态与相关实现。保留已有未提交改动。

| 项目 | 状态 | 决定 |
| --- | --- | --- |
| 预设配色 | 已修复 | 默认／紫苑只改卡片；其余调整卡片、选区、播放头；保留用户自定义覆盖 |
| E：真实边缘与共享拖拽 | 已修复 | 靠近哪一真实边缘只亮该手柄；同轨共同边界同时亮相对两手柄才联动；单手柄不拖邻居 |
| F：三层音频贴片 | 已修复 | 最多三层同时重叠，无内部滚动；拦截造成第四层的编辑，保留现有素材和撤销逻辑 |
| 验证、文档与生成 | 已修复 | 分层验证通过；源码、说明与生成页面一致，见最终验证记录 |
| 提交与推送 | 进行中 | 用户已明确授权；只推 origin/my-feature |

## 验证记录

- 配色：Chromium `cue-cards.spec.mjs --grep "preset cards"` 1/1 通过，七套主题截图已生成；确认默认／紫苑提示色数值未变，自定义覆盖保留。已查看小铃深色与灵梦浅色实际界面。
- E 首轮 4 项浏览器测试：1 通过、3 失败。发现真实共同边界恰在换行处时另一手柄检索范围过小；已扩展到已渲染的相邻行。另一个失败为旧吸附模式状态文字断言，单侧拖动现显示实际调整时间。继续复验。
- E 复验 4/4 通过：整块选中不点亮手柄、左右单侧、共同边界跨行双亮、单侧拖动不动邻居、共享拖动、间隙不共享、撤销、Alt 与按住手柄 A/D。拖动显示沿用同一命中集合；真实边缘跨行时动态增删手柄。
- F：音频核心单元测试 14/14；Chromium 两项定向用例通过。实际验证三层全部显示／内部不能滚动、插入／批量粘贴／拖动／键盘移动／裁剪／恢复完整音频拒绝第四层、不改变数据或撤销步数、有效操作仍可撤销重做；工程导入容量校验通过。静音贴片也计入三层，首尾相接不算重叠；总贴片数量仍可超过三条。超过容量的工程整体拒绝导入，不截断、丢弃素材或修改文件格式。

- 产物首轮：受限环境中的虚拟环境 Python 启动器报 permission denied，未生成页面；通过工具自动审批重试本地构建与验证。
- 完整 Python 首轮：1524 项，1461 通过、62 跳过、1 条旧样式资源断言失败（旧固定亮度与卡片选中背景）；改为当前主题变量与卡片底色规则后定向复验。JS 单元 296/296 通过。
- 提交前核对既有 WIP：发现清理空字幕调用普通成对删除，可能删除有文字的绑定另一侧并创建多步历史；先解绑待删除空段再无额外历史删除，保留有文字字幕。素材搜索显隐的初始化调用也修正到启动路径。增加实际菜单与撤销／刷新验证。
- 浏览器完整回归首轮 171 项：168 通过，3 失败。副字幕绑定边界用例仍在双手柄区域测试旧的单侧行为，改为明确命中单手柄；键盘用例误打开遮挡波形的全局设置，删除无关打开步骤。TTS 刷新在轮询未结束时会丢失请求，保留待刷新延迟并在当前请求结束后执行，继续复验。

## 最终验证

- JavaScript：`node --test tests/test_editor_utils.mjs tests/test_waveform_js.mjs tests/test_msw_audio.mjs tests/test_msw_audio_render.mjs`，296/296 通过。
- Python：`python -m unittest discover -s tests -p test_*.py` 首轮 1524 项中 1461 通过、62 跳过，1 个资源用例仍检查旧写法。更新该用例中旧固定亮度、背景和仅主轨表情包检测断言；`test_waveform.py` 复验 17 项通过（含 2 跳过），`test_editor_assets.py` 19/19 通过。累计全套无未解决失败，1462 通过、62 跳过。
- Chromium：卡片、键盘、多字幕、TTS、翻译、贴片剪贴板共 171 项，首轮 168 通过；3 项失败修正后连同 6 项空工程／清理用例复验 9/9 通过。补充轨道手柄隔离、真实跨行手柄、素材搜索重载及 TTS 轮询／取消／重试／三层限制，7/7 通过。累计 179 个不同浏览器用例分轮通过。
- 交互截图：已查看深／浅主题卡片、换行共同边界双手柄、三层配音全部可见的真实 Chromium 截图。图片存于仓库外。
- 产物：`python edit.py --blank` 成功，生成文件与最终前端源码一致；所有改动 JS/MJS 语法、30 个文件 UTF-8/LF／无 BOM、`git diff --check` 与新增文件／私有路径检查通过。
- 文档：编辑器指南、贴片指南、JSON 契约说明和 CHANGELOG 已同步。三层上限是编辑器容量限制，不改变文件 schema 或后端音频导出格式。
- 边界：使用本地合成媒体及模拟翻译／TTS 服务；未测试真实云端调用、其他浏览器、安装包或远端 CI。上述 62 项 Python 跳过项未宣称通过。
