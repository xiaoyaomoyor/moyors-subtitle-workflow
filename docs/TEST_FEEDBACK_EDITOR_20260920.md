# 编辑器生产反馈修正（2026-09-20）

基线：e20804f。开始时已有未跟踪 `.test-appdata/`，保留不动。本轮不提交、不推送。

| 编号 | 工作 | 状态 | 决定与验证 |
| --- | --- | --- | --- |
| 1 | 视频导出精度与源媒体音画尾差 | 已修复 | 原始时长统一四舍五入至毫秒，视频范围以服务端画面终点为基准，支持原声尾差提示与主动定格保留；8 组前后端共享夹具、真实音画尾差导出及手动范围拦截通过。 |
| 2 | ASR 颜色组引用及旧工程兼容 | 已修复 | 引用名称随 ASR 映射保留；波形沿组首兜底取色；再次识别继承旧颜色时也保留名称。Python、Node、浏览器回归通过。 |
| 3 | 颜色组斜纹圆点改空心 | 已修复 | 空心色点保留跳转组首和悬浮提示；浏览器确认无斜纹背景、使用 inset 圆环。 |
| 4 | 音频批量操作计数下沉 | 已修复 | 每分类底部左侧小字计数，按钮靠右；无选中时禁用贴片操作。浏览器与截图通过。 |
| 5 | 补齐／替换详情分别保留最近一批 | 已修复 | 各保留一批，详情区限高 180px 滚动；30 条结果浏览器验证通过；进度不清空详情、不重置详情滚动。 |
| 6 | ASR 范围重试可取消 | 已修复 | 直接发起新任务，旧记录显示正在重试，新记录可取消；防重复点击与跨工程响应。模拟服务验证两次提交、两次取消，未修改字幕。 |
| 7 | 删除媒体菜单补齐字幕入口 | 已修复 | 已移除并解除旧入口事件绑定；保留批量面板入口。 |
| 8 | 源音频预览增益 | 已修复 | 独立 Web Audio 增益节点，−60 至 +12 dB，支持设置／增减；当前会话试听，不写入工程或导出。切换媒体／工程复位。单元与浏览器通过；真实设备听感未测。 |
| 9 | 文件菜单整理与检查素材气泡 | 已修复 | 检查移至内容；恢复收纳至加载内容；检查结果使用临时气泡；真实恢复草稿与气泡浏览器验证通过。 |

## 验证边界

使用隔离应用数据目录和合成媒体，不读取本机密钥文件，不调用真实收费服务。分别记录单元测试、浏览器验证及产物生成结果；测试不能代替真实听感。

- 第一批：`node --test tests/test_msw_audio_render.mjs` 14/14；`python -m unittest test_msw_video_render.VideoTailContractTests` 通过（8 组共享场景）。Python 沙箱启动首次失败（uv trampoline permission denied），使用获准的提升运行重试成功。
- 第二批：音频渲染、试听、批量核心、波形、ASR 共五个 Node 文件 107/107 通过。面板与菜单实现完成，已补充浏览器回归用例；Python 导出／ASR 回归运行中。
- 第三批：Python 首轮旧测试把 `duration_ms` 当作显式延长指令，产生 1 失败、1 错误；按新契约改用明确 `end_ms`，并补齐测试进程 FFmpeg 环境后，视频／ASR／音频计划／导出服务／前端产物共 69/69 通过（无跳过）。浏览器第一轮 15/15：重试取消、颜色和空心点、计数、补齐、替换／取消／失联重试、独立源增益、菜单气泡、视频与 OTIOZ 导出均通过。
- 截图检查发现分类脚注继承通用上下外边距导致面板偏高，已压缩分类间距和脚注外边距；新增 30 条详情滚动、900×600 窗口及原生音画尾差导出验证，待完成。
- 补充浏览器 4/4 通过：草稿设置、恢复入口、30 条详情与 900×600 布局、真实 4.032 秒原声／4 秒画面的导出。默认导出 4 秒且无需尾部确认，手动指定 4.020 秒会要求尾部策略；真实生成文件经 FFprobe 核对。

## 收尾

- 本轮全部 9 项已实现，无阻塞项。未做真实云端 ASR/TTS 调用与物理设备听感测试；语音服务使用本地模拟，视频导出使用真实 FFmpeg。
- 最终重建：`.venv/Scripts/python.exe edit.py --blank` 成功；`test_editor_assets.py` 19/19 通过；修改的关键 JS 均通过 `node --check`；ASR 核心最终复验 16/16；`git diff --check` 通过。基线已有 `.test-appdata/` 保留，未提交、未推送。
- 回归汇总：Node 五文件 107 项、Python 五模块 69 项通过；浏览器两轮分别 15/15、4/4（其中批量详情在压缩布局后复验一次，共 18 个不同用例）。浏览器证据保留于工作区外层隔离检查目录，不将个人媒体或截图加入仓库。
