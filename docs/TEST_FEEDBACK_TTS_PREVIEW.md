# TTS 布局与统一试听

本轮保留前两轮未提交修改，不提交或推送。

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| P1 | IndexTTS 来源、预设、情感、语言、搜索选择与整排语速 | 已修复 |
| P2 | 参考音频使用素材库播放器，复用百炼／油库里可用试听 | 已修复 |
| P3 | 核对百炼语速能力，说明上下文、音色 ID 与两类保存 | 仅说明 |

已核对当前分支、实际 diff、上一轮 TTS 工作区记录和源码。百炼当前使用 Qwen3 HTTP API；官方文档只为 Instruct 系列提供描述式语速控制，没有可直接套用到普通 Flash 的数值语速参数。验证结果随实现回写。

P1：第一行来源／预设，第二行情感／语言，来源切换与名称／语言搜索共用音色卡框，当前音色和试听同行。搜索不改变已选参考；配音预设直接选择即应用，语速在末尾占满一行。脚本语法通过；首组 Chromium 21 项全部通过（IndexTTS 11、百炼 10），包含窄屏布局、所有情感方式、预设恢复和参考试听。新增针对搜索／排列／延迟试听的回归继续随 P2 验证。

P2：三个引擎共用素材库播放器，统一处理播放互斥、旧请求丢弃和工程切换。百炼 48 个系统音色引用官方试听 URL，仅保存地址；声音设计复用已生成的本机预览。油库里新增受原有 Token／同源校验保护的固定短句试听接口，限制同时一个请求、服务关闭可取消，不建立任务或素材。IndexTTS 音色与情感参考均复用已有文件。

验证：Chromium 34 个不同用例中 33 项首轮通过；官方示例进度拖动的模拟响应未实现 Range，补齐协议后该项独立复验通过。真实官方 CDN 的 HEAD 检查确认 `Accept-Ranges: bytes`；测试不下载真实示例、不调用付费合成。截图检查后将音色框试听按钮并入现有按钮主题。Node 六组核心 306 项全部通过。Python 全量 1315 项：1294 通过、19 跳过、2 项 Windows 回环连接中断；两组涉及文件独立复验结果如下补记。

P3：保持 Qwen3 HTTP API 的实际能力范围，普通 Flash／VC／VD 不虚构数值语速参数，Instruct 用声音描述调节。文档说明现有音色 ID 来自官方目录或账户内创建结果；MSWE 本机预设与 IndexTTS 服务预设分别维护。「保存合成设置」仅记住本机默认配音配置，按钮新增说明。Codex 上下文建议依据官方上下文说明；未找到 Astra 实验开关的足够公开细节，不声称它等于扩大窗口。

收尾：油库里 Python 15 项全部通过；素材导入 Python 8 项（7 通过、1 跳过）复验通过，前述 Windows 回环中断未再次出现。新增媒体播放打断未完成试听的覆盖；最后 Chromium 15 项全部通过（含两类迟到结果丢弃、主题布局、原素材试听与编辑／删除／导入回归）。所有修改脚本语法、生产 Python 和新增测试 Ruff、`git diff --check` 通过；43 个修改／新增文件发布扫描无编码、密钥、私有路径或媒体异常。最终 `edit.py --blank` 在隔离配置下重新生成，无手改内联副本。

本轮没有真实百炼计费调用，没有对用户已运行的 IndexTTS 服务发起推理；云端与 Index 行为使用模拟 API，油库里使用真实离线资源。没有待处理或阻塞项；未提交或推送。

最终生成产物校验：`test_waveform.py` 共 17 项，15 通过、2 跳过。
