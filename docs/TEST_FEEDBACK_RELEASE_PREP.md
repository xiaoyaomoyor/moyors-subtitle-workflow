# MSW 1.6.0-beta.1 发布准备

## 授权与基线

维护者要求按品牌建议整理项目，暂以自有 GitHub 仓库作为官网；保留其已经修改的文档，核查修缮后提交并推送 `my-feature`。准备与上游同版本号的五种平台安装包，另两项源码归档由 GitHub 提供。本轮不创建 tag 或公开 Release，不同步上游。

基线：`f7bdf7c`，上一轮图标、LLM 与素材库改动尚未提交。新增用户修改为 README 的“我的字幕流 / ASR 与 TTS”定位和历史 README 格式调整，已阅读实际 diff。保留正式中文名与定位，按发布需要继续完善。

| 项目 | 状态 | 范围 |
| --- | --- | --- |
| R1 品牌与产品入口 | 已修复 | GitHub 官网、帮助／反馈／关于、中文名、双语介绍及品牌展示；浏览器复验纳入 R4 |
| R2 文档与可选环境 | 已修复 | 用户导航、安装升级、环境清单、历史说明归档，移除过时体验凭据说明 |
| R3 跨平台发行流程 | 已修复 | 五包命名和门禁、包内容清单、实际导入冒烟、版本与固定标签文档链接；执行验证见 R4 |
| R4 验证与修缮 | 已修复 | Python / JS / 浏览器、发布资源与本机可执行检查，记录平台边界 |
| R5 提交并推送 | 已修复 | 编码／敏感内容与暂存 diff 检查通过，`04c33d8` 已推送 `origin/my-feature` |
| R6 跨平台构建预演 | 进行中 | 对已推送提交运行 build-only Actions，核验五个候选包；不创建 tag 或 Release |

品牌截图仅使用合成演示数据，不使用用户项目、媒体或配置。运行时模型和资源按功能独立安装，不纳入默认应用包。历史文件优先归档和整理导航，避免为清理而破坏追溯或兼容性。

R1：已保留用户正式中文名和 ASR / TTS 定位，更新双语 README；官网、错误反馈及帮助入口指向 MSW 仓库；新增“关于”帮助页，复用原浮窗。已生成并检查演示数据截图，图标遵循原稿。版本标记避免在中间换行。远端默认分支已只读核对为 `my-feature`，未发现目标标签。

R2–R3：新增文档目录、安装升级、可选环境和构建发布说明；归档旧上游版本正文与 MSW 开发明细，正式版本条目集中描述本项目功能。更新随包帮助与服务文件清单，避免携带测试缓存；五种安装包缺任一项会阻止上传。手动 Actions 只生成候选包和说明预览，公开发布仅由版本标签 push 触发。未新增模型、没有删除历史文件。Python 当前环境未安装 PyYAML，工作流 YAML 解析验证改用现有开发工具继续检查。

R4 阶段记录：现有 Playwright 内置 YAML 解析器已解析工作流，并核对手动运行不发布、五包门禁；31 份当前用户／开发文档的本地链接检查通过。新增发行契约 9 项通过，Node 核心 306 项通过。隔离 Windows 构建环境完成 PyInstaller 核心包构建，EXE `--smoke-import` 返回 0。首轮全量 Python 1322 项有两处旧断言不匹配（新增关于标签和完整发布门禁），已更新；同时高负载下素材接口一次超时，相关文件隔离复验通过，继续收尾回归。

R4 最终本机证据：使用 `PYTHONUTF8=1`、隔离配置和已安装的 FFmpeg／油库里资源，Python 全量 1322 项通过（20 项按可选环境条件跳过）；Node 六组核心测试 306 项通过；Chromium 品牌、启动器、翻译、三引擎 TTS、素材试听和音视频导出共 127 项通过。Ruff 全仓库检查通过。生成版编辑器、31 份文档链接、YAML 发布门禁和版本检查通过。

Windows 隔离 PyInstaller 核心包构建成功；EXE `--smoke-import` 返回 0，实际启动临时本机 Server 后，页面内嵌品牌和使用会话令牌的素材能力接口通过。检查期间未读取真实 Key、未调用付费 API 或修改现有 IndexTTS 服务。一次未设 UTF-8 的复验按 GBK 解码子进程输出失败，已在与 CI 相同的 UTF-8 环境复验通过，不修改生产逻辑掩盖环境错误。

边界：本机 EXE 为核心打包冒烟，不等于含 bootstrap、可选环境清单和 FFmpeg 的最终五包；macOS／Linux 将由推送后的构建预演验证，目标平台上的真实桌面验收仍需分别进行。当前不创建 tag 或 Release。

R6：首次预演 `34350755205` 基于 `04c33d8`。Windows 与 macOS 在相同三处持久化断言失败：实际返回规范化路径，测试预期保留临时目录的短路径或别名。已将预期路径规范化，保留保存位置、素材内容和恢复引用的原有校验；不改变生产保存逻辑。首页版本徽标纳入 prerelease，下载链接统一进入版本列表。继续运行候选构建。
