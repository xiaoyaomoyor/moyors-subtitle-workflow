# 波形右键菜单反馈

用户要求先提交并推送前几批工作，再调整本轮菜单。已推送 `60c4f91` 到个人仓库 `origin/my-feature`，未修改 main。提交检查发现原方案文档 Markdown 行尾空格，清理后通过。

| 问题 | 状态 | 决定 |
| --- | --- | --- |
| 1 空白处菜单 | 已修复 | 按指定五项排序；新增时间选区在右键处添加默认片段并打开精确时间弹窗，保留其他选区。 |
| 2 选区菜单样式 | 已修复 | 复用通用菜单样式，清除为红色，设置带窗口图标，ASR 标注。 |
| 3 字幕优先与 TTS | 已修复 | 字幕块优先命中字幕菜单，删除前增加 TTS 入口，移除时间选区附加项。 |
| 4 音频贴片菜单 | 已修复 | 统一样式、删除红色及快捷键、音量增益文字对齐，移除加选项。 |

按顺序修改和验证；本轮修改先保留工作区待审阅，不自动包含在此前提交中。

空白菜单：4/4 浏览器测试通过（五项顺序、新增并编辑片段、三种工具下冻结播放头加选）。涉及 editor.js、msw-range-selection.js、i18n 与时间选区测试。

阶段汇总：空白菜单与选区菜单已完成。选区样式/操作 4/4 浏览器用例通过，验证字体尺寸、文字颜色、内边距一致，清除为危险色并显示 Esc，设置带窗口图标。当前处理字幕菜单优先级和 TTS 入口。

字幕菜单：主字幕多选、副字幕两例浏览器测试 2/2 通过。选区不再拦截字幕/贴片/空隙块；主副字幕菜单的 TTS 入口均在删除之前，打开正确操作对象、保留选中内容且不自动提交。全局追加选区菜单项已移除，仅空白处显式添加对应操作。

贴片菜单首轮样式测试失败：菜单定位到第一项中心后，测试将悬停高亮项与未悬停增益行比较颜色。改为比较同处普通状态的第二项；实际字体 13px、内边距 7px/14px 已一致，继续补验。

贴片菜单复测 1/1 通过：选区内优先命中贴片、文字位置/颜色/字体/内边距一致、红色删除及 Delete、窗口图标、增益修改、删除后撤销恢复。四项代码已完成，开始完整相关回归及产物、截图检查。旧多字幕/空隙测试同步适配菜单删减，保留创建轨道、空隙算法和撤销覆盖。

## 最终验证

- 4 项反馈全部已修复，无待处理或阻塞；本轮菜单修改尚未提交，前一批已经按用户要求提交并推送。
- Playwright（chromium，workers=1）：editor-time-range、waveform-clipboard、editor-tts-workspace 全部 40/40 通过；multi-subtitle 两项创建/空白菜单和 editor-upgrade-d 两项空隙回归通过。总计 **44 个不同浏览器用例通过**。截图补验与 Esc 补验分别通过，不重复计数。
- Esc 关闭贴片菜单保留时间选区；选区自己的清除仍使用 Esc。新增片段在末尾裁短，不越出时间轴。
- Node：node --test tests/test_msw_tts.mjs tests/test_msw_audio.mjs tests/test_waveform_js.mjs，74/74 通过。
- Python：python -m unittest test_editor_assets，19/19 通过。已运行 python edit.py --blank，本机页面与便携页面均参与浏览器验证。
- 视觉检查：已查看空白、选区、主字幕多选与音频贴片菜单截图。空白菜单初次截图处于淡入阶段，补充等待 opacity=1 后截图并确认。贴片菜单的字体、危险色、窗口图标和增益行对齐正常。
- 源码与生成产物：16 个本轮修改/新增文件通过 UTF-8/LF、无 BOM、本机路径扫描、JavaScript 语法和 git diff --check；源码文档与 CHANGELOG 已同步。
- 提交：HEAD 与 origin/my-feature 均为 60c4f91eb8b8943bac40267846551513543edc62，main 仍为 bc5262cd7f9f972d1efc299d4537fd13eaa657ce。
- 未验证边界：未重新打包 EXE、未跑全仓库/跨平台 CI。本轮没有发起真实云端识别或合成，菜单入口只打开调用面板。
