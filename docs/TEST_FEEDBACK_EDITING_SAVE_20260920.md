# 编辑操作与保存反馈调整

基线 HEAD：51d7f33；包含上一轮已验证、尚未提交的导出优化。本轮用户要求六项调整后统一提交推送。保留 `.test-appdata/`，不读取个人 `.env`。

| 项目 | 状态 | 决定与证据 |
| --- | --- | --- |
| 1 合并窗口选项背景 | 已修复 | 与缩放／偏移共用字段背景样式；浏览器断言两者背景一致且非透明通过。 |
| 2 波形跟随源试听增益 | 已修复 | 已接入设置、工作区保存与 dB 读数；原生／ReaPeaks 共用绘制倍率，rAF 合并可见行刷新；浏览器验证 -12dB 改变绘图、保留缓存对象，关闭后图形恢复。 |
| 3 新工程保存位置命名 | 已修复 | 新工程窗口及本机保存选择器使用新名称，已有工程另存为保留原意；浏览器名称切换与 Python picker 标志转发通过。 |
| 4 粘贴颜色 | 已修复 | 已实体化继承颜色并重映射插入／副轨排序后的原引用；同时平移副本字词时间；主副轨分别验证副本颜色、原引用和字词时间通过。 |
| 5 缩放偏移范围、反馈及时间同步 | 已修复 | 已加入轨道／范围／联动选项，面板状态与连续手势撤销合并；整体平移保持间隔、字词同步；密集字词帧模式重复保存幂等、主副批量绑定移动及快捷键撤销通过。 |
| 6 保存即时气泡 | 已修复 | 普通保存、服务另存与浏览器文件写回已接通即时加载气泡；序列化之前让浏览器绘制完整反馈，自动保存成功静默；延迟响应、并发编辑、失败及另存窗口层级通过。 |

## 验证

使用隔离配置和合成媒体；阶段结果随实现回写。最终检查生成产物、回归、差异及远端分支。

- 首轮新浏览器用例 9/10 通过，失败项为关闭缩放窗口后输入焦点阻挡 Ctrl+Z；已在浮动窗口关闭时释放内部焦点。
- 第二轮 12/13 通过，撤销及主副轨绑定批量移动通过；新增密集字词用例发现帧取整造成保存快照不幂等。已改为保留字词毫秒真值、只计算辅助帧字段，复验中。
- Python 保存／导出／资源契约第一轮 58 项运行、56 通过、2 类音视频测试因未传 FFmpeg 路径跳过；将以显式合成测试工具路径补跑。

## 最终回归

- Python 第一轮 56 项通过；补充显式 FFmpeg 路径后，音视频实际渲染及工程契约 58 项全部通过。
- Node `test_editor_utils.mjs`、`test_waveform_js.mjs`、`test_msw_audio_render.mjs`：298 项全部通过。
- 浏览器新用例 13 项、浏览器新工程保存 7 项通过；语言／保存回归首次 7/9 通过，补齐遗漏翻译并将旧 `Saved!` 断言更新为新文案后余下两项通过。
- 额外的服务另存进度、主副字幕合并与绑定联动共 3 项通过。既有偏移测试原先要求字词时间不变，与这次同步字词时间的修复矛盾；已改为验证等比重映射和完整撤销，复验通过。本轮共验证 33 个不同浏览器用例。
- 截图检查合并选项背景、缩放范围与状态布局、即时保存气泡；保存气泡取消淡入等待，避免大工程序列化时反馈尚未完全显现。
- 仅使用隔离数据和合成媒体。未进行收费 ASR/TTS 调用、小时级工程压力、跨平台原生选择器或安装包验收；浏览器原生文件选择器标题由浏览器控制，应用详情窗口与后端选择器已命名。无仅说明或阻塞的请求项。
- 已重新生成 `blank-editor.html`；最终资源检查 19 项通过，修改文件的 UTF-8／LF、JS 语法与 `git diff --check` 通过。上一轮导出优化与本轮六项调整一并提交至 `my-feature`；原有 `.test-appdata/` 不纳入提交。
