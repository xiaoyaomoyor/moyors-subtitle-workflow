# 复制本文件为 .env 并填入实际值（.env 已被 gitignore，不会提交）
# 用于 generate_subtitle_qwen_api.py（Qwen / Fun-ASR 异步文件转写）
# API Key 申请：https://help.aliyun.com/zh/model-studio/get-api-key

DASHSCOPE_API_KEY=

# 地域：beijing（华北2，默认）或 singapore（新加坡）
DASHSCOPE_REGION=beijing

# 业务空间 ID：北京地域选填（填写后使用推荐的专属域名），新加坡地域必填
DASHSCOPE_WORKSPACE_ID=

# 可选：默认语言（不填则自动识别）。不同模型支持范围不同，取值如 zh / en / ja / ko / de / fr
DASHSCOPE_DEFAULT_LANGUAGE=

# 可选：GUI 界面语言（默认 zh，可选 zh / en）
MAW_GUI_LANG=zh

# 可选：GUI 记忆上次选择的模型与语言（空语言 = 自动识别）
MAW_GUI_LAST_MODEL=
MAW_GUI_LAST_LANGUAGE=

# 可选：GUI 是否显示相对小众的语言（默认 false，仅显示 8 种常用语言）
MAW_GUI_SHOW_RARE_LANGS=false

# 可选：Launcher 外观（system / light / dark，默认跟随系统）
MAW_GUI_THEME=system

# 可选：本地 ASR 模型缓存根目录；Launcher 也可以在本地模型面板中选择
MAW_MODEL_CACHE_ROOT=

# 可选：OCR 独立运行环境目录；Launcher 也可以在 OCR 设置中选择
MAW_OCR_RUNTIME_ROOT=

# 可选：旧 Qwen3 是否启用字级时间戳（默认 true；Qwen-Audio/Fun-ASR 固定开启）
DASHSCOPE_ENABLE_WORDS=true

# 可选：Qwen 是否启用 ITN（默认 false；Fun-ASR 由模型固定处理）
DASHSCOPE_ENABLE_ITN=false

# ===== Qwen-Audio 3.0 ASR（可选） =====
# 预编译热词表 ID：必须在百炼按 qwen-audio-3.0-asr-flash-filetrans 创建
DASHSCOPE_QWEN_AUDIO_VOCABULARY_ID=

# hotwords.txt 的即时热词权重：1-5 或 50；默认 5。仅 Qwen-Audio 使用
DASHSCOPE_QWEN_AUDIO_HOTWORD_WEIGHT=5

# 可选：UTF-8 context 文件路径；每次最多发送 400 字符
DASHSCOPE_QWEN_AUDIO_CONTEXT_FILE=

# ===== Fun-ASR 热词（可选） =====
# 必须在百炼按 Fun-ASR 目标模型创建，不能复用 Qwen-Audio 的 vocabulary_id
DASHSCOPE_FUNASR_VOCABULARY_ID=

# ===== 自定义 OpenAI 兼容 ASR（可选） =====
# 默认使用 OpenAI 官方服务；也可以填写其他兼容 POST /audio/transcriptions 的服务。
# Base URL 可填根地址或带 /v1 的地址，MAW 会请求其 /audio/transcriptions。
MAW_OPENAI_ASR_BASE_URL=https://api.openai.com/v1
MAW_OPENAI_ASR_MODEL=whisper-1
MAW_OPENAI_ASR_API_KEY=

# 可选：轮询间隔（秒，默认 5）
DASHSCOPE_POLL_INTERVAL=5

# 可选：轮询超时（秒，默认 1800）
DASHSCOPE_POLL_TIMEOUT=1800

# 可选：FFmpeg 路径（可填 ffmpeg.exe，或 ffmpeg/ffprobe 所在 bin 目录）
FFMPEG_PATH=


# ===== Soniox STT（可选第二供应商，generate_subtitle_soniox_api.py 使用）=====
# API Key 申请：https://console.soniox.com
SONIOX_API_KEY=

# 可选：模型（默认 stt-async-v5）
SONIOX_MODEL=stt-async-v5

# 可选：轮询间隔（秒，默认 3）
SONIOX_POLL_INTERVAL=3

# 可选：轮询超时（秒，默认 1800）
SONIOX_POLL_TIMEOUT=1800

# ===== 腾讯云录音文件识别（可选） =====
# SecretId 可在 Launcher 中填写；SecretKey 请只放在本机 .env 或系统环境变量
TENCENT_SECRET_ID=
TENCENT_SECRET_KEY=
TENCENT_APP_ID=
TENCENT_REGION=ap-guangzhou
TENCENT_ASR_ENGINE=16k_zh_en_2.0
TENCENT_POLL_INTERVAL=3
TENCENT_POLL_TIMEOUT=1800


# ===== Launcher 字幕后处理 LLM（可选）=====
# 也可在 Launcher 右上角的 ⚙️ 配置 -> LLM 后处理中保存这些设置。
# 字幕后处理会把字幕文字发送到所选第三方 LLM；时间码、媒体路径和 API Key 不会写入请求正文。
# API Key 仅保存在本机 .env。URL / MODEL 可留空使用 Launcher 内置默认值。
MAW_POSTPROCESS_DEEPSEEK_API_KEY=
MAW_POSTPROCESS_DEEPSEEK_BASE_URL=https://api.deepseek.com
MAW_POSTPROCESS_DEEPSEEK_MODEL=deepseek-v4-flash
# 思考强度：auto / off / low / medium / high；auto 跟随模型默认值
MAW_POSTPROCESS_DEEPSEEK_REASONING_MODE=off

MAW_POSTPROCESS_ZHIPU_API_KEY=
MAW_POSTPROCESS_ZHIPU_BASE_URL=https://open.bigmodel.cn/api/coding/paas/v4
MAW_POSTPROCESS_ZHIPU_MODEL=glm-5.2
MAW_POSTPROCESS_ZHIPU_REASONING_MODE=off

MAW_POSTPROCESS_QWEN_API_KEY=
MAW_POSTPROCESS_QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
MAW_POSTPROCESS_QWEN_MODEL=qwen-plus
MAW_POSTPROCESS_QWEN_REASONING_MODE=off

MAW_POSTPROCESS_CUSTOM_API_KEY=
MAW_POSTPROCESS_CUSTOM_BASE_URL=
MAW_POSTPROCESS_CUSTOM_MODEL=
MAW_POSTPROCESS_CUSTOM_REASONING_MODE=off

# Launcher 记忆上次选择的后处理供应商：deepseek / zhipu / qwen / custom
MAW_POSTPROCESS_LAST_PROVIDER=deepseek

# ===== 必剪 ASR（实验性第三供应商，generate_subtitle_bcut_api.py 使用）=====
# 非官方免费接口，无需 API Key；可能随时变更、失效或触发限流，请勿高频调用
# 可选：单文件时长上限（秒，默认 7200；不建议上调）
BCUT_MAX_AUDIO_SECONDS=7200

# 可选：轮询间隔（秒，默认 3；硬下限 2，配低了会被抬回下限）
BCUT_POLL_INTERVAL=3

# 可选：轮询超时（秒，默认 1800）
BCUT_POLL_TIMEOUT=1800


# ===== 表情包默认路径（HTML 编辑器使用）=====
# 未传 -s/--stickers 参数时，edit.py / generate_subtitle_*.py / regen_html.py 从此读取
# 支持嵌套子目录（如 大狗/、Nox/ 等子文件夹会被递归扫描）
STICKER_DIR=
