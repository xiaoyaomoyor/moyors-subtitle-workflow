# 模块外观与悬浮信息调整

用户要求以素材库外框为基准统一其他模块；本次调整工作区外观，不改变项目数据。开始时 my-feature 工作区干净。

| 项目 | 状态 | 决定 |
| --- | --- | --- |
| 模块外框 | 已修复 | 其他模块使用素材库同样的浅色 1px 描边、圆角与边界裁切 |
| 选择与播放头线宽 | 已修复 | 卡片与字幕块选中描边统一为 2px；播放头线由 2px 调为 1.5px |
| 主副标题反馈 | 已修复 | 选中侧整个可见标题使用强调色与下划线 |
| 更多悬浮显示 | 已修复 | 保留旧开关保存值，补齐副字幕及所有对象总用时；用户确认试听音量使用贴片与配音轨叠加增益；字幕只显示序号与时间 |

## 验证记录

- 待验证：真实浏览器模块边界、主副选择、悬浮内容、窄布局及生成产物。

- 模块外框：Chromium 1/1 通过，核对 1280px 和 900px 布局各模块描边／颜色／圆角一致，保留现有弹层裁切规则。

- 描边：首轮源码使用 1.5px，浏览器取整后卡片仍为 1px；最终统一 2px，去掉字幕块外侧额外深色 1px 硬描边；贴片选中样式保留。浏览器交互合并于标题与悬浮检查。

- 主副标题：由 current-target 改为真正的 selected 状态，首字及可见的「字幕」后缀一起强调与下划线；不强调序号。待同批浏览器核对切换选中与宽窄布局。

- 首轮悬浮验证：主副字幕与标题 2/2 通过，贴片未显示详情。定位到原代码向不存在的 processing-host.options 读取开关，已改用波形 timeline 的动态接口；复验中。

- 接线修复后 Chromium 3/3 通过：主副切换、完整标题／缩略标题、起止与总用时、贴片裁剪及 -5 + -3 = -8 dB、轨道静音与关闭详情。选中描边最终采用 2px 避免 1.5px 轮廓在普通像素密度下取整为 1px；字幕块取消额外硬边和外移。

- 回归 18 项初轮：15 通过，3 个旧断言落后于基线（OCR 旧提示、两个工具、模块清单计入隐藏素材库及旧菜单名称）。仅更新测试契约，复验中。Node 290/290、Python 波形 17 项（2 跳过）及资源 19/19 通过，便携产物已生成。

## 完成

- 四项已修复；布局断言复验 4/4 通过，累计涉及 19 项浏览器用例均已通过（含单独贴片悬浮用例）。
- Node 290/290；Python 波形与资源 36 项、34 通过／2 按条件跳过；语法、差异和 UTF-8/LF 检查通过。
- 已查看模块外框及完整主副标题选中截图，证据在仓库外；已生成 blank-editor.html。
- 本次仅修改外观、显示信息及对应验证，未改变字幕或配音内容。

## 后续调整：字幕悬浮行序

- 状态：已修复。主、副字幕共用的悬浮格式改为第一行文本、第二行序号与起止时间（总用时），与音频贴片一致；关闭「更多悬浮显示」仍只显示文本。
- 修改：`web/waveform.js` 的 `cueHoverTitle`，同步更新已有浏览器断言。
- 验证：Chromium 的 `more hover details show both subtitle tracks and total duration` 用例 1/1 通过，覆盖主副轨及关闭详情；已重新生成 `blank-editor.html`。
- ASR 与素材库升级本轮仅制定计划，见 `PLAN_ASR_ASSET_LIBRARY.md`，尚未实施。
