# 素材库升级 A–C 执行记录

本轮仅执行已批准的 A、B、C。保留上轮模块外观与悬浮提示的未提交修改；D–G 后续功能不在本轮实施，尤其不接入贴片 ASR、不增加批量 ZIP 或音频重新生成按钮。

| 阶段 | 状态 | 内容 |
| --- | --- | --- |
| A | 已修复 | 核心与前后端契约完成；保存/恢复复验继续随 C 进行 |
| B | 已修复 | 公共素材库、类型筛选、多选与试听栏设置完成 |
| C | 已修复 | 复制、独立编辑、插入/拖入、配对、冲突与保存恢复验证通过 |

## 事实基线

- 分支 `my-feature`。开始时保留 13 个已跟踪文件修改及两份计划/反馈文档。
- 原音频素材 `msw.assets` 使用独立文件和专用删除历史，普通字幕撤销保留音频库存。
- 新字幕素材使用 `subtitle_assets` 与 `asset_batches`，不会混入音频读取/混音器。
- 用户确认一条字幕一卡片；放入时间线以播放头为锚点，保留所选字幕相对间隔。

## 验证记录

- A：首轮 3 个 Node 核心用例因毫秒创建日期超过初设数值上限失败；已改用前后端一致的安全整数校验。复验 3/3 通过，Python 工程契约 2/2 通过。
- A–C 首轮 Chromium 3/3：复制、文本独立编辑、撤销/重做、序列化、主副配对插入、筛选清选、时间线选择退出素材编辑与设置保存通过。
- 原音频/TTS 回归 10 项：9 通过；1 项断言仍预期两个设置，新增试听栏后为三个。已同步预期，待复验。
- 已查看素材库截图，卡片选区描边及颜色标记正常，数量提醒在同一行，原模块布局保留。
- B 回归复验：设置、隐藏试听栏后卡片继续播放/暂停、音频导入与 TTS 创建/导出 4/4 通过。
- C 扩展首轮：3 项失败。两项因自动保存让共享测试工程带入上一用例素材，改为每用例独立工程/服务；副轨选择断言需等待 dialog 关闭事件完成。隔离后 7/7 通过，含真实拖放、冲突跳过、副轨选择、跨页多选、窄布局、空时间线保存重开。
- 核心 Node 初轮 XML 测试因沙箱无法启动 Python 失败；指定项目 Python 并允许子进程后 293/293 通过。Python 字幕素材 2、工程兼容 6、资源 19，共 27/27 通过。
- 恢复草稿及恢复后另存为通过；原另存为用例增加素材复制后保留了字幕编辑目标，直接修改 DATA 的旧测试步骤被编辑器正常提交覆盖。改为通过真实文字框编辑，待复验。
- 另存为复验又遇到既有自动保存正在写入时手动保存返回 false 的测试时序；测试先关闭自动保存、等待已有写入完成，再执行明确保存。原另存为与继续保存最终通过，没有修改生产保存逻辑。
- 便携产物实测通过：无后端、无源媒体时可复制/独立编辑/插入字幕素材，TTS 仍明确不可调用。

## 完成汇总

- A、B、C 均已修复；D–F 未实施，G 仅执行本轮功能所需的针对性回归。
- Chromium 涉及 20 个不同用例，最终均通过：新素材库 7、原音频/TTS 回归 10、另存为与恢复 2、便携编辑器 1。
- Node 293/293；Python 27/27；主副素材独立性、时间/颜色/绑定、冲突、重复结果及格式校验有核心覆盖，浏览器覆盖实际复制、编辑、拖动、播放、选择与保存。
- 真实云端 ASR/TTS 未调用；TTS 回归使用现有模拟服务。本轮没有新增云端能力。
- `blank-editor.html` 已由源码重新生成；已检查普通与窄布局截图。源码/产物语法、UTF-8/LF 与 diff 检查通过。
- 保留之前的 WIP；未提交、未推送。
