# ASR 调用布局与选区交互反馈

保留 my-feature 的现有未提交修改，不修改 main，不打包、提交或推送。

| 问题 | 状态 | 决定与证据 |
| --- | --- | --- |
| 1–3 ASR 两列布局、调用配置、识别记录 | 已修复 | 服务、模型、语言等调用参数回到面板；连接参数留在环境配置；保存与开始相邻；预览归属每条记录，四个操作按钮保持底部独立。 |
| 4 精确时间选区弹窗 | 已修复 | 调整布局与键盘确认。 |
| 5 选区边界跨行拖动 | 已修复 | 复现并验证跨行扩大、缩小及取消。 |
| 6 源音轨下拉框 | 仅说明 | 选择媒体文件中的音频流；影响试听、波形和 ASR，eng 是媒体语言标签。 |

基线：已读取上一轮反馈、当前源码和实际 diff；现有大批 WIP 保留。验证结果逐项补充。

## ASR 验证过程

- 已拆分环境与调用保存；后端保留旧接口兼容，旧 JSON 中的连接参数不能覆盖新环境配置。
- 第一轮浏览器 20 例：19 通过；取消用例仍直接点击已折叠记录内的按钮而超时，已补充先展开记录的真实操作。继续验证新增的多记录预览、折叠后导出目标。
- Python 19 例：17 通过、2 因未指定 FFmpeg 跳过；下一轮带工具路径补齐。
- 已查看本轮 ASR 面板与环境配置截图，两列布局、440px 宽度及页脚显示正常。

ASR 补验 2/2 通过，累计 21 个不同浏览器用例已通过；识别记录截图已检查，折叠后导出仍对应选中的片段。当前转入精确时间弹窗。

## 阶段汇总

ASR 1–3 与精确时间弹窗 4 已完成。弹窗的 3 个浏览器用例通过：两列尺寸、毫秒精度、Enter 应用、空值和越界拒绝、Esc/取消保留原选区、切换工程关闭弹窗。已查看实际截图，源码和 blank-editor 已同步。当前复现选区边界跨行行为。

## 跨行边界复现

- 垂直拖到相邻行、反向缩小、移动起点保留终点的测试已通过。
- 修复前越过行末测试失败：原几何下行末被轨道头偏移挤出视口，9800ms 边界无法抓取；将测试行设为完全可见后，拖出行尾仍停在 10000ms，确认时间计算钳制。
- 已修复：多行宽度扣除轨道头间距；边界拖动允许横向越过行末，同时按鼠标所在行定位；两侧对称计算边界命中宽度。

跨行修复后：时间选区与字幕/贴片剪贴板 28/28 浏览器用例通过；行尾手柄可见与末行比例专项 1/1 通过。跨行截图已检查。Node ASR/波形单元测试 68/68 通过。所有反馈代码项已完成，进入最终产物与相关回归检查。

## 最终验证与边界

- 代码反馈 1–5 均已修复，第 6 项仅解释现有音轨选择语义，无阻塞项。
- 浏览器累计 **63 个不同用例通过**：ASR 22、时间选区 16、波形剪贴板 13、媒体导入 6、TTS 工作区 6。第一轮取消用例的折叠操作适配、修复前行末钳制失败均已复测通过；最后一组相关回归 19/19 通过。
- 命令：隔离环境下运行 Playwright 的 editor-asr、editor-time-range、waveform-clipboard、editor-media-import、editor-tts-workspace（chromium，workers=1）。ASR 初轮 20 + 取消/记录补验 2 + 最终筛选回归 19；选区/剪贴板 28 + 行尾专项 1，按用例名称去重计数。
- Node：node --test tests/test_msw_asr.mjs tests/test_waveform_js.mjs，68/68 通过。
- Python：python -m unittest test_msw_asr test_msw_media_service test_msw_media_jobs test_editor_assets，指定隔离配置及真实 FFmpeg，62/62 通过；此前缺工具路径的 2 项跳过已补齐。
- 产物：已运行 python edit.py --blank；本机 Server 与便携页面实际浏览器测试通过。已查看两列 ASR、环境配置、Soniox 条件参数、识别记录、精确选区弹窗、跨行选区和完整行尾截图。
- 源码审计：77 个修改/新增文件 UTF-8、LF、无 BOM、无个人机器路径；JavaScript 语法及 git diff --check 通过。
- 文档：EDITOR_ASR.md 与 CHANGELOG.md 已同步。my-feature 保留原 WIP，main 引用未变；未提交、推送或重新打包 EXE。
- 未验证边界：使用真实 FFmpeg 与本机模拟 ASR 服务，没有发起真实云端收费识别；未重跑全部仓库/跨平台 CI，未验证新版发行包。
