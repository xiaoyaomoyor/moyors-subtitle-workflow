# 编辑器 B 阶段：百炼 TTS 与素材库

## 事实基线

- 用户授权实现 B：媒体菜单 TTS、字幕范围与连锁对象选择、百炼设置、默认隐藏的素材库、生成后在右下打开、与现有风格一致。
- 开始时分支 `my-feature`，HEAD `624219c`，工作区干净。现有 A0/A1 任务服务与翻译实现已在仓库中；不合并上游、不提交、不推送。
- 百炼使用 Qwen3 非实时 HTTP 接口；北京／新加坡地域、模型、系统音色、语言与指令模型参数按官方文档核对。真实密钥不读取，测试使用独立配置和模拟服务。

## 实施清单

| 编号 | 内容 | 状态 | 证据与边界 |
| --- | --- | --- | --- |
| B1 | TTS 适配器、设置、逐条任务、取消与部分失败 | 已修复 | 最终完整 Python 通过，含协议、密钥隔离、参数、逐条可用、取消、跨重试成功项去重及容量前置检查 |
| B2 | 素材保存、工程引用、重新打开与 WAV 导出 | 已修复 | Range、同目录保存、ZIP 内容、路径与跨工程隔离通过；浏览器试听、导出、保存和重开通过 |
| B3 | 连锁选区规则、媒体菜单与配置界面 | 已修复 | 5 项选区单测通过；真实浏览器验证双轨先选、绑定映射、配置复用、任务操作与便携禁用 |
| B4 | 素材库布局、分页搜索、试听、右下自动打开 | 已修复 | Chromium 6/6 通过（26.9s），105 条分页/搜索/关闭恢复通过；首次结果及完成时显示，已有停靠位置保留 |
| B5 | 回归、真实浏览器、便携产物、文档 | 已修复 | Python 1192 项（19 跳过）；Node 287 项；Chromium 30 项通过。已生成便携页，schema、README、changelog 与使用文档同步 |

## 验证记录

`python -m unittest discover -s tests -p test_msw_tts.py`：6 项通过（0.517s），仅使用合成 WAV 和模拟 HTTP 客户端。包括素材重开、另存为、缺失和 schema 校验。仓库虚拟环境需工作区外解释器，已获自动审批执行；没有读取真实 `.env`。

真实云端合成与完整发行包构建尚未验证。

阶段汇总（B1–B2）：后端复用原任务队列；每个音频文件独立写入并登记，原工程保存前先复制相对路径素材。素材库可导出 JSON 工程与 TTS WAV 的 ZIP（原视频仍按现有媒体引用关联）。取消及重启不自动重发请求，重试核对已保存素材以覆盖落盘与任务进度之间的中断窗口。

阶段汇总（B3–B4）：媒体菜单与原工具窗同风格，素材库接入五模块停靠树；旧布局不重排，新模块默认隐藏。72 项 Node 定向测试全部通过，翻译与 TTS Python 定向共 22 项通过。首轮浏览器 5/6（自动打开与手动恢复相遇）；调整只在首次结果/完成显示并等待完成状态后，6/6 全部通过。新增输入表与逐条结果表，避免大量字幕反复序列化全量快照；旧任务在启动时迁移。

接口依据：[Qwen-TTS API](https://help.aliyun.com/zh/model-studio/qwen-tts-api)、[非实时语音合成](https://help.aliyun.com/zh/model-studio/non-realtime-tts-user-guide)。

## 最终验收

- 完整 Python：`python -m unittest discover -s tests -p "test_*.py"` 的隔离驱动版本运行 1192 项，`OK (skipped=19)`，62.106s。隔离驱动屏蔽真实 `.env`，保留原默认路径契约，并为子进程注入独立配置；没有连接真实云服务。首轮唯一失败为旧脚本清单未包含新增 TTS 模块，补充清单后完整回归通过。
- Node：`node --test tests/test_editor_utils.mjs tests/test_waveform_js.mjs tests/test_msw_translation.mjs tests/test_msw_tts.mjs`，287 项全部通过。`editor.js`、`waveform.js`、新 MSW 脚本与 i18n 语法检查通过。
- 真实 localhost + Chromium：`playwright test editor-tts editor-translation editor-i18n-save open-project-attach --grep-invert "English locale covers"`，30 项全部通过，1.4m。其中 TTS 9 项验证保存配置、双轨对象、边编辑、撤销、取消晚到、部分重试、105 条分页搜索、窗口恢复、切工程隔离与英文窄窗口；其余 21 项保护既有翻译及保存。初次扩大验证发现自动显示面板插入撤销记录，现已修复；切工程测试也补全了真实的打开文件操作。
- 素材库自动显示不进入字幕撤销历史；用户主动调整窗口仍沿用原布局历史。素材库存独立于字幕历史，不会因为撤销或重做重复计费。
- 使用仓库解释器运行 `edit.py --blank`，从唯一源 `web/` 重新生成 `blank-editor.html`。核对新脚本与内联产物一致，29 个本任务文本文件均为 UTF-8、LF、无 BOM，`git diff --check` 通过。`ruff check maw/msw server-editor/serve.py` 通过。
- 已目视检查素材库与英文 900×700 窗口截图；素材显示在右下，长列表在模块内部滚动，非试听状态收起播放器以保留列表空间。截图保存在工作区外的可视化目录，不入仓库。
- 工作保留在 `my-feature` 的未提交更改中，没有同步上游、提交或推送。

| 未验证／仅说明事项 | 状态 | 边界 |
| --- | --- | --- |
| 真实百炼输出、账号权限、额度与云端网络 | 仅说明 | 使用模拟 HTTP 和合成 WAV 验证；未读取真实密钥、未发起付费合成 |
| 完整发行包运行 | 仅说明 | 本轮未构建安装包；打包清单／依赖契约已通过完整 Python 回归 |
| 原全页面英文遍历用例 | 仅说明 | 沿用 A0 记录的旧搜索占位符断言不匹配，未纳入 30 项；新增 TTS 英文界面检查通过 |
| 音频贴片、混音与本地 TTS 引擎 | 仅说明 | 保留为后续阶段；当前音频是独立素材 |

## 2026-09-08 实测反馈：WAV 音频不完整

| 内容 | 状态 | 证据与边界 |
| --- | --- | --- |
| 定位三次合成失败的实际阶段 | 仅说明 | 只读本机任务状态与错误：三次均为北京、Flash、Cherry，单条下载后在 `audio_info` 报「WAV 音频不完整」。说明已通过密钥校验与云端请求，不能归因于 API Key 留空或额度耗尽；没有读取真实配置／密钥 |
| 兼容生成音频的 WAV 长度占位值，保留截断检测 | 已修复 | 二次实测日志为声明 2147483546 字节、读取 241920 字节。已用同样数值复现旧实现失败；扩展识别接近 2／4 GiB 的窄区间，覆盖扣除头部长度的变体，普通长度不符及采样帧截断仍拒绝。最终 Python 1200 项、Node 276 项及浏览器试听／导出通过 |
| 原始云端文件形态与用户重试结果 | 仅说明 | 失败下载未保留，已知最新长度信息，但完整原始头部未知；回归使用构造样本。本轮未请求百炼或改变真实密钥，修改后真实云端复测仍需重启本机服务并手动重试单条 |

验证记录：

- `python -m unittest discover -s tests -p "test_*.py"` 的隔离驱动运行 1198 项，`OK (skipped=19)`，53.618s；新增长度占位值、元数据块、普通截断、HTTP 下载长度、采样帧及实际任务入库覆盖。
- `node --test --test-reporter=dot tests/test_editor_utils.mjs tests/test_waveform_js.mjs tests/test_msw_tts.mjs`，276 项通过。首次沙箱执行的既有 XML 测试无法启动工作区外 Python，获自动审批后同组回归通过。
- `playwright test tests/e2e/editor-tts.spec.mjs --project=chromium --grep "blank TTS key"`，1 项通过（6.9s）。真实本机测试服务器和 Chromium，模拟流式 WAV：ASR 假密钥留空复用、选择连锁副字幕、Cherry、自动素材入库、正确试听时长、下载 WAV 头与采样数据完整。
- 编辑器和波形 JavaScript 语法、相关 Python Ruff 与 `git diff --check` 通过。此次未改前端源码，无需重新生成便携页；没有重启或中断用户正在使用的服务。

二次反馈验证：

- 构造 241920 字节 PCM，分别声明 2147483546／2147483547 字节；后者经过 Python 按采样帧向下取整也会报前者数值。修复前两种情况均复现用户原文错误；修复后得到正确的 120960 个采样帧，保存后的音频样本和元数据保持一致。
- 不再枚举三个固定值，改为识别 32 位有符号／无符号最大值下方各 64 KiB 的占位范围；与 32 MiB 下载上限远离，普通长度不符仍失败。首轮边界回归发现区间多包含一个边界值，收紧后重跑全套通过。
- 同一隔离 Python 全套命令：1200 项，`OK (skipped=19)`，66.502s；同一 Node 命令：276 项通过。Ruff、语法和 `git diff --check` 通过。
- 同一 Chromium 回归改用本次接近 2 GiB 的长度值，1 项通过（4.7s），验证空 TTS 密钥复用 ASR、选择副字幕、素材入库、正确时长试听和 WAV 导出。未修改前端源码或便携页，未重启用户服务、未提交或推送。
