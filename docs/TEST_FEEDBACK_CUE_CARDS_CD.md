# 字幕列表 C/D 阶段与描边反馈修正

2026-09-14：用户要求修正整卡播放头命中、选择侧与关联侧描边，并继续 C/D。保留 A/B 与其他 15 个未提交／未跟踪文件，不提交或推送。

| 项目 | 状态 | 决定 |
| --- | --- | --- |
| C：播放头与选择反馈 | 已修复 | 实际命中主或副轨且整卡未选中时整卡实线；任一侧选中后选中侧实线、关联侧虚线，播放头不覆盖；时间间隙清除命中 |
| D：翻译所选字幕入口 | 已修复 | 主／副及多选右键菜单在 TTS 上方打开现有字幕翻译窗口，不自动提交；沿用绑定副字幕映射主字幕、未绑定不触发全量翻译的既有规则 |
| D：联动选择默认关闭 | 已修复 | 新设置默认 false，兼容已保存的 true/false；不修改主副绑定与时间跟随规则 |
| 验证与生成 | 已修复 | 浏览器命中、选择、菜单与默认值回归通过；语法、276 项单元测试和 19 项资源契约通过；已从源码生成空白页 |
| 后续波形边缘手柄与三层贴片限制 | 仅说明 | 留后续阶段 |

## 验证记录

开始时已核对 AGENTS.md、A/B 记录、git status 与实际改动。以下保留分阶段执行记录，最终结果见末尾。

- C 首轮：`node --check web/editor.js` 通过；Playwright 卡片选择、整卡命中与真实播放共 3/3 通过。覆盖主轨单独命中、副轨单独命中、重叠命中、时间间隙、独立副字幕、暂停重绘、单轨显示、双排选择与播放退出。命中变化才更新 DOM，列表重建时主动恢复命中；不逐帧扫描整张列表。

- D 翻译入口：Chromium 1/1 通过，真实右键主／副波形块与列表多选；验证菜单在 TTS 上方，主字幕选择数量正确，副字幕按绑定映射，未绑定副字幕范围为 0 且不能开始，无翻译请求提交。

- C/D 首轮完整浏览器回归 121 项：115 通过、6 项失败均为旧联动默认开启时的选中数量断言（预期 2／4，实际 1／2）。依赖联动的特定用例改为显式开启，其余按新默认验证真实选中数量；新默认值及 true/false 刷新保留用例通过。字幕/波形单元测试 276/276 通过。

## 最终结果

- C/D 全部完成，无阻塞。清理旧的单主轨 active 缓存，播放命中统一使用时间与主副两轨；保留导航／字幕预览原有逻辑。暂停重绘、纯配音时钟与播放更新入口都接入同一命中刷新，命中键不变时跳过 DOM 更新。
- 选择优先级：任一侧实际选中 → 选中侧实线、关联未选侧虚线；整卡未选中 → 主或副轨实际命中时整卡播放头色实线；没有实际命中 → 无播放头描边。单主／单副、独立字幕、双列／双排同样适用，不改变背景色。
- 右键翻译复用已有窗口与范围规则，主／副波形块及列表多选均覆盖；不自动开始、不额外收集凭据。副字幕翻译仍基于对应主字幕，未绑定副字幕提示并忽略。
- 新配置的主副联动选择默认 false；已保存 true/false 保持。绑定时长跟随、拆分、合并与显式多选的功能未取消。

| 验证层 | 命令／范围 | 结果 |
| --- | --- | --- |
| 语法与差异 | `node --check` 前端及新增测试、`git diff --check` | 通过 |
| 单元测试 | `node --test tests/test_editor_utils.mjs tests/test_waveform_js.mjs` | 276/276 |
| 浏览器完整范围 | Playwright `cue-cards.spec.mjs`、`multi-subtitle.spec.mjs`、`editor-translation.spec.mjs`、`waveform-clipboard.spec.mjs` | 121 项分轮通过：首轮 115；6 项默认值断言更新后复验通过 |
| 定向复验与补充 | 上述 6 项、2 项播放反馈、1 项副字幕滚动、click-behavior 1 项、cue-color-filter 2 项 | 12/12 |
| 资源契约 | `python -m unittest discover -s tests -p test_editor_assets.py` | 19/19 |
| 生成页面 | `python edit.py --blank` | 已从最终源码生成 |
| 编码 | UTF-8、LF、无 BOM | 通过 |

已人工查看仓库外浏览器截图：仅副轨命中时整卡播放头描边、副字幕选中时主字幕虚线。浏览器使用 Chromium 和合成本地媒体；翻译测试使用本地模拟服务。未做真实云端翻译、其他浏览器或安装包／CI 验证。

保留 A/B 和任务开始时的其他 WIP，未提交或推送。本次仅完成 C/D，波形边缘手柄与贴片三层限制留后续阶段。
