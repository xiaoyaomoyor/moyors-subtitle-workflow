# ASR 与素材库 D–G 执行记录

用户要求先提交推送 A–C，再依次完成 D–G。基线 `c7ef713` 已推送到用户仓库的 `origin/my-feature`；main 与 upstream 不变。

| 阶段 | 状态 | 内容 |
| --- | --- | --- |
| D | 已修复 | 音频贴片来源、裁剪提取、范围前置与任务批次 |
| E | 已修复 | 手动覆盖/入库、删除搁置与候选导出 |
| F | 已修复 | 所选音频 ZIP、原配方重新生成并只入库 |
| G | 已修复 | 契约、浏览器与既有流程回归、文档和便携产物 |

## 验证

Python ASR 21 项、Node ASR/素材核心 17 项通过。首轮浏览器 ASR 18 项通过、5 项失败：4 项为旧自动应用/布局预期，已更新；1 项发现点击 ASR 面板清掉贴片选择，已补面板选择保护，待重跑。云端流程使用模拟服务验证，不读取或调用用户真实凭据。

D/E 复验：ASR 浏览器 23 项均已通过（20 项整组通过，3 项服务适配器更新手动应用预期后单独通过）。Node 增加同批部分完成、时间重基与入库去重用例后 18 项通过。整体视频覆盖改为仅影响视频时段，边界切穿字幕阻止覆盖；重叠贴片批次在提交时记录冲突，即使部分任务尚未完成也不允许覆盖。

F 验证：新素材生成、未知响应继续确认、原贴片替换/取消/三层限制共 7 项浏览器用例通过；实际下载的 ZIP 包含且仅包含所选两条 WAV。导出测试先修正选择接口调用，再改为检查浏览器下载产物（DevTools 响应体未提供完整下载数据）。Python ZIP 3 项通过，包含选择去重、中文安全文件名、跨工程拒绝、内容变化和大小限制。截图检查发现生成提示误显示「停止后续导入」，已按导入状态隐藏。

G 中间验证：Node 全部 349 项通过。Python 全部 1558 项首次运行 1507 通过、48 跳过、3 失败，失败均为显式 FFmpeg 路径覆盖了 GUI 测试自身的临时路径；清除本轮注入变量后 GUI 模块 245 项中 244 通过、1 跳过，三项失败消除。另有字幕/音频跨模块浏览器 9 项通过，后续最终结果继续补记。

## 最终结果

- ASR 与字幕素材浏览器整组 **31/31 通过**，含无源视频重叠贴片分别识别、同批入库，以及非零裁剪和非零时间线起点。
- TTS 生产与音频素材最终组 **16/16 通过**，含草稿切分/清除/部分失败、另存为、便携 HTML、贴片补齐字幕、增益/静音、素材新增与 ZIP、原贴片重新生成及三层限制。此前额外覆盖恢复、分页、音频拖动及项目切换。
- Node 核心新增整体视频末尾边界与 44.1 kHz 贴片快照用例，相关 20/20 通过；全量共 351 个用例。Python 合计 1510 通过、48 跳过（包含上述环境变量纠正后的复验），无剩余失败。
- 已检查 ASR 两列布局、素材卡片五个图标和多选描边截图；生成提示不再显示无关的停止导入按钮。
- 更新 ASR/素材库使用文档、JSON 契约、CHANGELOG 与阶段计划，重新生成 `blank-editor.html`。文件统一 UTF-8、LF；语法与 `git diff --check` 验证通过。
- 真实 FFmpeg 用于提取和时间验证，供应商服务均为本机模拟，未实际调用用户云端 ASR/TTS。D–G 修改保留在工作区，尚未提交推送；此前 A–C 的 `c7ef713` 已推送。
