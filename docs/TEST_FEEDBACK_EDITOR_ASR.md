# 编辑器独立工作流实施账本

2026-09-12 用户批准 [实施方案](dev/MSW_EDITOR_INDEPENDENT_WORKFLOW_PLAN.md) 的全部推荐，按四批依次执行。本次基线 `my-feature@4a639fe`，初始仅有该方案未跟踪文件，产品代码无 diff。继续在当前分支工作，不同步上游、不提交、不推送或发布。

## 已批准的决策

- D1：先验证 Qwen／Fun-ASR API，再接现有正式云端入口；本地实验 ASR 后续处理，不新增引擎。
- D2：空主轨无冲突自动加入；已有字幕先预览再应用。
- D3：片段边界切穿旧字幕时显示冲突，可扩展到完整字幕边界后识别或调整范围，不自动丢弃外侧文字。
- D4：保留旧副字幕及音频，解除失效绑定并标记过期，不自动重新生成。
- D5：临时工程直接导入，首次保存选择位置；本机选择引用源文件，浏览器拖放必要时缓存。
- D6：独立打开编辑器入口，保留 Launcher 默认入口和批处理用途。

## 进度

| 阶段 | 状态 | 实施／验证 |
| --- | --- | --- |
| P0 基线和接口 | 已修复 | 已核对实际代码、干净产品 diff 和确认的 D1–D6；接口沿用方案第 3 节及现有 MSWE 桥接／MSW 命名空间 |
| P1 空工程和时间轴 | 已修复 | Python 契约 75、Node 290、Chromium 23 全部通过；空工程纯 TTS、混合主副剪切和撤销已验证，便携页面已重建 |
| P2 媒体导入 | 已修复 | 导入、保存重开、源音轨和代理的真实 FFmpeg／Chromium 回归通过；代理保持原素材身份 |
| P3 后台波形 | 已修复 | 36 项 Python 通过（含三小时流式分析、取消及缓存）；4 项浏览器回归通过，工具行已修正并截图检查 |
| P4 时间选区 | 已修复 | 7 项范围浏览器＋13 项原手势／贴片回归通过，Node 波形 54 通过；画布反色已截图检查 |
| P5 ASR 任务 | 已修复 | Python 100、Chromium ASR 6 项通过；真实 FFmpeg 提取＋模拟云端，任务持久化／取消／候选导出已验证 |
| P6 结果应用 | 已修复 | Node 11、Python 契约 20＋新增映射回归 15、Chromium 13 项通过；范围替换、旧副字幕复核、一次撤销与幂等验证通过 |
| P7 入口和交付 | 已修复 | 最终需求验收 60 项通过；Node 323／Python 全套及最后并发回归通过；Windows 冻结标准／lite 验证通过，既有浏览器失败和平台边界见下 |

## 接口约定

媒体登记绑定工程 ID、会话及媒体版本；源文件与播放代理分开。时间选区保存源整数毫秒，属于视图状态。ASR 返回候选段，应用只改变目标字幕及关联状态，不加载输出文件整份覆盖工程。空白可视空间不成为真实峰值或导出时长。配置继续使用已有本机配置服务，不把 Key 写入工程。

## 验证记录

### 最终验收（2026-09-13）

四批代码与文档已完成，当前仍为 `my-feature@4a639fe` 的工作区修改，`main@bc5262c` 未改变；没有提交、同步上游、推送或发布。

| 层级 | 执行及结果 |
| --- | --- |
| Python 全套 | 隔离驱动执行 `unittest discover -s tests -p test_*.py`：1390 项，1378 通过、12 按环境跳过；200.040 秒 |
| 最后并发／持久化检查 | `test_msw_media_service/test_msw_media_jobs/test_msw_asr/test_msw_persistence/test_editor_entry/test_packaging_contract`：85 项全部通过，28.720 秒；包含后台探测不占保存锁、换媒体后迟到探测不发布、双窗口共用媒体身份 |
| Node 全套 | `node --test tests/test_*.mjs`：323 项全部通过，最终一次 1.390 秒 |
| 本次需求最终 Chromium 验收 | `editor-asr/editor-media-import/editor-time-range/editor-empty-timeline/editor-tts-workspace/waveform-clipboard/waveform-deletion/fcp7-export`：60 项全部通过，3.1 分钟；ASR 含 15 项，英文任务进度文案补齐后另复测 1 项通过 |
| 保存竞态补充回归 | 另存为期间继续编辑，副本落盘后立即保存：重复 2 次均通过。错误原因为后台 FFprobe 持有保存锁，现已移到锁外并在发布媒体身份前重新校验 |
| 完整 Chromium 套件 | 初次全量执行 478 项：419 通过、49 失败、10 跳过（28.2 分钟）。失败项另以 Git HEAD 旧前端、同一后端和测试夹具对照：48 项中 45 仍失败、3 通过。3 个实际回归（波形刷新导致节点重建、另存为后保存、FCP7 假时长）已修复并定向复测；另一个计数布局测试已按当前设置菜单位置校正并通过。45 个旧前端亦可复现的失败不在本次功能中扩大修复范围，完整套件不声明全绿 |
| Windows 产物 | 最终 PyInstaller 冻结包构建成功；lite 无 FFmpeg 直接 `--editor --blank --no-open --port …` 启动、能力目录可用且工具状态为未就绪；标准包使用自带 FFmpeg/FFprobe，完成二进制媒体导入、后台波形、真实冻结 OpenAI 子进程调用本机模拟 HTTP、片段整数毫秒回映射和应用前校验，未启动 Launcher |
| 源码与文档 | `blank-editor.html` 从 `web/` 重建并同步验证包；73 个改动文件通过 UTF-8/LF、无个人机器路径、JS 语法及 `git diff --check`；Windows 打包脚本通过 PowerShell 语法解析，macOS 启动脚本通过 `bash -n` |

源码启动方式：`uv run python maw_gui.py --editor --blank`。Windows 发行包新增 `Start-Editor.vbs`，macOS 包装新增 `Start-Editor.command`，Linux AppImage 原有参数转发可直接使用 `--editor`。使用文档见 [编辑器 ASR](EDITOR_ASR.md)。

未验证边界：没有调用真实云端 Key／额度，也未进行云端识别精度评估；macOS、Linux 的原生选文件窗口与实际包启动需对应平台验收。本地实验 ASR／隐藏服务未开放。变帧率、特殊封装、所有帧率与异常媒体的组合未穷举；已验证真实 FFmpeg、多音轨、源时间偏移、片段提取、长波形与取消。现有浏览器回归积压主要涉及旧菜单入口、快捷键／布局预期、老 TTS 界面英文覆盖及连接恢复等待；后续应单独整理。

### 阶段过程记录

所有测试使用隔离配置和临时合成媒体，真实供应商与模拟服务分别记录。

- P7 集成中：Node 全套 323 项通过；Python 全套 1390 项完成，1378 通过、12 按环境跳过（200.040 秒）。首轮 Node 4 个 Python 子进程启动失败，使用可运行的隔离虚拟环境后全通过；Python 首轮暴露能力查询意外启动导出线程、脚本清单断言未更新、可选 Runtime 漏带 `gui_platform.py`，均已修复。三个 GUI 配置测试被验证命令的全局 FFMPEG_PATH 干扰，移除该覆盖后通过。Windows 两次 PyInstaller 构建成功，最新冻结 lite 的独立入口与能力目录检查通过；加入已有 FFmpeg/FFprobe 后，标准包真实冻结 OpenAI 子进程经本机模拟 HTTP 完成媒体分块导入、波形、片段提取、识别和源时间回映射，候选返回与应用前校验通过。没有打开 Launcher 或使用真实 Key。
- P7 浏览器全量回归仍在执行。Git HEAD 的只读旧前端对比已确认 3 个既有失败：窄媒体窗跳转按钮已隐藏、拖动面板时文本区现已随面板增高、设置按钮已移入菜单。后续按新功能回归、陈旧测试预期和旧问题分别记录，不将全量执行表述为全通过。另已修复后台波形到达时重套工作区布局，并让旧测试夹具携带真实源指纹，避免无关缓存刷新干扰手势测试。导出路径补查发现 FCP7／OTIO 仍读可视占位长度，现已改用真实源时长；普通字幕派生输出使用实际内容时长。

- 第三批完成：ASR 独立选区、任务、预览和范围替换已接通。应用保留范围外字幕、旧副字幕、素材和贴片，解除失效绑定；复核状态随工程保存／撤销。Node 11 项通过；Python 工程／ASR 20 项通过，增加颜色分组源时间映射后 ASR 15 项通过。Chromium 13 项全部通过（1.2 分钟），包括自动加入、手动替换、撤销／重做、目标编辑冲突、空结果、源文件变动、响应丢失重试只提交一次、删除颜色组头后保留范围外颜色。截图检查后调整说话人复选框排列、提交后折叠配置以显示结果。纯函数验证素材和贴片不被修改、来源提示撤销恢复；实际云端仍未调用。

- P5：复用现有 JobManager、Launcher 参数校验和转写子进程，ASR 使用独立快照类型；不启动自动翻译／TTS／HTML／峰值副作用。精确提取 16 kHz 音轨片段并只回映射一次段／词时间，凭据只在运行时和原本机配置中，任务 JSON 无密钥。Python ASR／处理／原转写 100 项全部通过（10.317 秒）；Chromium Qwen 全段、边界扩展、迟到取消 3 项通过（12.8 秒），Fun-ASR／Soniox／OpenAI 配置适配追加 3 项通过（13.4 秒）。供应商 HTTP 为本机测试夹具，没有真实云端精度／额度验证。首轮配置目录测试发现必剪仍是可见免 Key 入口，按正式 API 范围排除非官方免 Key 必剪；隐藏腾讯和实验本地也不在本批开放。

- P4：独立整数毫秒选区、Ctrl/Cmd+Shift 捕获、反向／跨行／虚拟行自动滚动、占位／真实画布反色、单行平移、Esc／pointercancel、精确输入和范围右键菜单已接入。首轮 5 个测试暴露行编号栏造成约 428 ms 偏移，改为实际波形行的左缘与完整行宽计算；重跑范围＋既有剪贴板手势 18 项通过（40.5 秒），再补单行峰值到达／贴片上拖选 2 项通过（7.3 秒）；Node 波形 54 项通过。截图确认只反色波形画布，标签、字幕和工具栏保持原色。Cmd 等效仅在 Windows Chromium 模拟修饰键验证，真实 macOS 仍待平台验收。范围不发起 ASR，后续面板完成前对应右键项禁用。

- P3 最终定向验证：配置 PATH 后 Python 媒体任务／波形／扩展契约 36 项全部通过，三小时峰值为 1,080,000 对、base64 小于 3 MiB，真实解析中途取消通过。新工具行第一轮浏览器测试暴露绝对定位覆盖（2 失败），已修正工具行位置与各布局的波形顶部留白，重新生成便携页面后 Chromium 4 项全通过（15.9 秒），重新查看截图确认控件与波形无覆盖。整套回归及发行包仍留到 P7。

- 第二批阶段汇总：新增受控媒体登记与持久化、原生选择、分块导入、媒体分析单工作线程、独立播放代理和源音轨选择。Python 媒体任务／原波形／MSW 契约 41 项（39 通过、2 项依赖 PATH 的旧 FFmpeg 测试跳过）；新增多音轨测试显式使用真实 FFmpeg，已通过有声／静音轨波形及代理音轨一致性。Chromium 导入＋后台分析 4 项全部通过（16.3 秒）。已查看真实浏览器截图，发现原工具栏控件挤压换行，调整为独立可横滚媒体工具行，待复测。没有调用真实 ASR 供应商。

- P2：媒体服务／处理／持久化 Python 共 35 项（34 通过，未配置 FFmpeg 时跳过 1）；指定 FFmpeg 后媒体服务 7 项全部通过。Chromium `editor-media-import` 首轮暴露临时工程保存后播放路径遗漏，已修复；两处测试辅助调用也已修正，恢复后重新执行 3 项全部通过（18.4 秒）。原生引用、浏览器分块、取消回退、空工程保存重开与 HTTP Range 字节一致已验证。兼容播放代理、多音轨和后台波形尚未验证。

- P1 已实现：全量主字幕删除／剪切、混合主副轨按稳定 ID 删除并合并撤销、清空隐藏副轨、零峰值时间轴及真实内容／显示时长分离。源码语法检查通过，Node 波形 54 项通过，便携页面已重新生成。
- P1 首轮 Chromium 新增 4 项：3 通过；隐藏副轨用例因测试辅助函数已自动加 `#`、调用方重复传入而失败，已修正测试选择器，待重跑。另补充混合主副轨身份与空工程 TTS 浏览器验证。
- Python 虚拟环境的 uv trampoline 在沙箱内无法启动，获自动审核允许后沙箱外运行现有 Python 3.11.16；测试只连接本机模拟服务。
- P1 最终：`editor-empty-timeline`、`editor-tts-workspace`、`waveform-clipboard` 共 23 项 Chromium 全通过（48.4 秒）；Python `test_project_contract/test_msw_project_schema/test_msw_persistence/test_msw_audio_plan/test_editor_assets` 75 项通过；Node 编辑器／音频／混音 236 项＋波形 54 项通过。`git diff --check` 通过。最初测试选择器失败已修正重跑，不遗留阻塞。全套 Python／发行包／长视频性能留到集成验收，不将本阶段定向测试等同于全套通过。
